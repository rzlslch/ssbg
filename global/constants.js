const DIALOG = 'dialog:';
const DIALOG_OPEN_DIR = `${DIALOG}openDir`;

const ENGINE = 'engine:';
const ENGINE_GENERATE = `${ENGINE}generate`;
const ENGINE_LOG = `${ENGINE}log`;

const MENU = 'menu:';

const MENU_LOAD_STATE = `${MENU}loadState`;

const MENU_LOAD_INDEX = `${MENU}loadIndex`;
const MENU_LOAD_LAYOUT = `${MENU}loadLayout`;
const MENU_LOAD_INCLUDE = `${MENU}loadInclude`;
const MENU_LOAD_POST = `${MENU}loadPost`;
const MENU_LOAD_PAGE = `${MENU}loadPage`;

const MENU_LOAD_SOMETHING = `${MENU}loadSomething`;
const MENU_LOAD_FILE = `${MENU}loadFile`;
const MENU_SAVE_FILE = `${MENU}saveFile`;
const MENU_CREATE_FILE = `${MENU}createFile`;

const TEST = 'test';

module.exports = {
  DIALOG_OPEN_DIR,
  ENGINE_GENERATE,
  ENGINE_LOG,
  MENU_LOAD_STATE,
  MENU_LOAD_INDEX, 
  MENU_LOAD_LAYOUT, 
  MENU_LOAD_INCLUDE, 
  MENU_LOAD_POST, 
  MENU_LOAD_PAGE,
  MENU_LOAD_SOMETHING,
  MENU_LOAD_FILE,
  MENU_SAVE_FILE,
  MENU_CREATE_FILE,
  TEST
}