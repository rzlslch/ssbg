const { getDir, get_post, get_layout, get_include, get_page } = require("../global/variables");

module.exports = async () => {
  // console.log(getDir(), get_post(), get_layout(), get_include(), get_page());
}